#
# Change these to find the tools to build sparc executables with
#
GCC := gcc
CC := cc
CXX := $(GCC)
AS := $(CC)
LD :=  $(CC)
SIZE := size
NM := nm
STRIP := strip
RANLIB := ranlib
AR := ar
LEX := lex

